## Начало работы
----
### Описание проекта

Данный проект носит **образовательный** характер и представляет собой поэтапное создание ядра операционной системы для архитектуры **x86**. Каждая новая ветка будет содержать документ с подробным (или не очень 😊) описанием реализованных модулей ядра.

### Подготовка проекта

В файле [README.md](../../README.md) перечислены необходимые инструменты для создания, отладки и тестирования ядра ОС. Этот документ объясняет процесс сборки, линковки и запуска проекта в эмуляторе [QEMU](https://www.qemu.org/).

#### Структура проекта *(будет меняться!)*  

```
📦 jOS  
├── 📂 arch  
│   └── 📂 x86  
│       ├── 📂 boot  
│       │   └── 🖥️ `boot.asm` – Ассемблерный код загрузки ядра  
│       └── 🔧 `toolchain-x86.cmake` – настройки кросс-компиляции для x86  
├── 📂 kernel  
│   ├── 🖥️ `kernel.cpp` – основной код ядра ОС  
│   └── 🔗 `linker.ld` – скрипт линковки для ядра  
├── 📂 Docs  
│   └── 📚 `Lesson 1. Quick start/Getting_started.md` – документация  
├── 📜 `CMakeLists.txt` – сборка через CMake  
├── 📜 `grub.cfg` – конфигурация GRUB для загрузки  
└── 📜 `README.md` – описание проекта  
```
Думаю, перед началом стоит разобраться с тем, как вообще происходит загрузка операционной системы. Оговорюсь сразу — объяснение будет максимально поверхностное, потому что по факту ~~я не понял, как это работает~~ часть загрузки происходит "под капотом", и от нас мало что зависит, раз уж мы используем GRUB.

Итак, давай по порядку — этапы загрузки и что я имел в виду под выбором инструментария:

Первое, что делает ПК после подачи питания — это аппаратная проверка POST (**P**ower-**O**n **S**elf-**T**est). Она завершается поиском загрузочного устройства (дискета, CD-ROM или жёсткий диск) в том порядке, в котором настроено встроенное ПО.

Затем BIOS проверяет диск на наличие сигнатуры загрузки. Она представляет собой «магическое число», которое находится в последних двух байтах сектора.\
У меня это `0x55` и `0xAA`:
<!-- 000001f0  00 00 00 00 00 00 00 00  00 00 00 00 00 00 55 aa  |..............U.|-->
Затем весь сектор (512 байт) загружается в оперативную память по адресу `0x7C00`.

Окей, и что потом? А потом начинается выполнение кода, который только что скопировали в оперативку. В нашем случае — это код загрузчика **GRUB**.
Теперь по поводу выбора инструментария. Я опирался на проблемы, описанные в разделе **[Loading](https://web.archive.org/web/20241012032953/https://wiki.osdev.org/Boot_Sequence)**, и честно — для меня это тёмный лес. Единственное, что я понял — GRUB реально упрощает жизнь.
Например:\
Если загрузка происходит с жёсткого диска, где загрузочная запись занимает всего 446 байт, то GRUB реализует **многостадийную загрузку**. Он сам включает защищённый режим, передаёт информацию о памяти и многое другое. Короче это натуральный **абсолютный кинотеатр**!

##### GRUB
**GRUB** — это первая программа, которая выполняется после включения компьютера. Как загрузчик, он отвечает за загрузку и передачу управления ядру операционной системы. Ядро, в свою очередь, инициализирует остальные компоненты ОС.

Конфигурация GRUB хранится в файле `grub.cfg`, где указывается, какое ядро и с какими параметрами нужно загружать. В нашем случае там прописана загрузка через стандарт **Multiboot2**, который позволяет гибко настраивать процесс (например, загружать ядро в графическом режиме или передавать дополнительные параметры).

<!-- menuentry "JustOS" {
    multiboot2 /boot/JustOS-X86
    boot
}-->

**Что это значит на практике:**
* **`menuentry "JustOS"`** — создаёт пункт меню с названием "JustOS"
* **`multiboot2 /boot/JustOS-X86`** — загружает моё ядро по стандарту Multiboot 2
* **`boot`** — передаёт управление загруженному ядру (эта команда неявно выполняется в конце каждого menuentry, но я указал её явно для наглядности)

**Когда я выбираю "JustOS" в меню GRUB, происходит следующее:**
* Выполняется команда multiboot2, которая загружает моё ядро в память.
* Если загрузка прошла успешно, автоматически выполняется boot.
* Управление передаётся в точку входа моего ядра.

##### Скрипт компоновщика

Так как мы используем **Multiboot2/GRUB** для загрузки ядра, нам нужно обеспечить, чтобы загрузочный заголовок находился в первых 32 КБ файла, а также имел выравнивание 8 байт:

> An OS image must contain an additional header called _Multiboot2 header_, besides the headers of the format used by the OS image. The Multiboot2 header must be contained completely within the first 32768 bytes of the OS image, and must be 64-bit aligned. In general, it should come _as early as possible_, and may be embedded in the beginning of the text segment after the _real_ executable header.

Чтобы добиться этих параметров, нужно написать скрипт компоновки `linker.ld`.

<!-- 
ENTRY(_start)

SECTIONS {
    . = 1M;

    .text : ALIGN(4K) {
        . = ALIGN(8);
        KEEP(*(.multiboot))
        *(.text)
    }

    .rodata : ALIGN(4K) {
        *(.rodata)
    }

    .data : ALIGN(4K) {
        *(.data)
    }

    .bss : ALIGN(4K) {
        *(COMMON)
        *(.bss)
        *(.stack)
    }
}
-->

**Внимание!** Хотя я сказал, что скрипт помогает обеспечить правильное расположение заголовка, на самом деле он делает не только это. Помимо этого, скрипт нужен для того, чтобы указать загрузчику, куда будет загружена операционная система и организована на уровне сегментов.

Давайте конкретно разберем про заголовок и его место в исполняемом файле. Когда у линковщика на руках имеется куча [объектных файлов](https://habr.com/ru/articles/478124/), нам нужно обеспечить нахождение заголовка `.multiboot` в самом начале исполняемого файла.

🔧 Как скрипт обеспечивает расположение заголовка в начале:

* 1. **Базовый адрес загрузки:**
```ld
. = 1M;  /* Ядро загружается по адресу 1 МБ в памяти */
```

* 2. **Принудительное размещение Multiboot заголовка:**
```ld
.text : ALIGN(4K) {
    . = ALIGN(8);           /* Выравнивание 8 байт для Multiboot2 */
    KEEP(*(.multiboot))     /* КРИТИЧЕСКИ ВАЖНО: разместить ПЕРВЫМ и СОХРАНИТЬ */
    *(.text)                /* Остальной код после заголовка */
}
```

* 3. **Механизмы обеспечения правильного порядка:**

- **`KEEP()`** — предотвращает удаление секции линковщиком, даже если на неё нет явных ссылок
- **Порядок перечисления** — секции размещаются в том порядке, в котором они указаны в скрипте
- **Выравнивание** — `ALIGN(8)` гарантирует 64-битное выравнивание, требуемое спецификацией Multiboot2

* 4. **Результат в бинарном файле:**
Как показал твой эксперимент с hexdump, Multiboot заголовок располагается:
- **По смещению 0x1000** от начала ELF-файла (после служебных заголовков)
- **В первых 32 КБ** файла, как требует спецификация
- **С правильным выравниванием** 8 байт



##### Ассемблерный код загрузки ядра

Файл `boot.asm` содержит начальный код инициализации ядра, который выполняет критически важные низкоуровневые операции подготовки системы перед передачей управления основной логике ядра на C++.

**Назначение и ключевые функции:**

- **Размещение Multiboot2 заголовка** - специальной структуры данных, которая идентифицирует ядро как совместимое со стандартом Multiboot2 и позволяет загрузчику корректно его загрузить
- **Базовая инициализация процессора** - настройка критически важных регистров и режимов работы
- **Подготовка стековой памяти** - создание корректного стекового кадра для последующего выполнения C++ кода  
- **Создание минимального окружения** - приведение системы в детерминированное состояние перед запуском высокоуровневого кода

**Структура кода:**

```asm
section .multiboot
header_start:
    dd 0xE85250D6                ; Идентификатор Multiboot2
    dd 0                         ; Архитектура (0 = x86)
    dd header_end - header_start ; Длина заголовка
    dd 0x100000000 - (0xE85250D6 + 0 + (header_end - header_start)) ; Контрольная сумма
    dw 0                         ; Тип тега (0 = завершающий)
    dw 0                         ; Флаги тега
    dd 8                         ; Размер тега
header_end:

section .text
global _start
extern kernel_main

_start:
    cli                         ; Запрет аппаратных прерываний
    mov esp, stack_top          ; Инициализация указателя стека
    call kernel_main            ; Переход к основной функции ядра
    hlt                         ; Остановка процессора (защита от возврата)

section .bss
stack_bottom:
    resb 16384                  ; Выделение 16 КБ для стека
stack_top:
```

**Принцип работы:**

После того как загрузчик (GRUB) обнаруживает и проверяет Multiboot2 заголовок, он загружает ядро в память по адресу 1 МБ, подготавливает базовое окружение и передает управление на метку `_start`. Ассемблерный код выполняет минимально необходимую инициализацию: отключает прерывания чтобы гарантировать бесперебойную работу на ранних этапах, настраивает стек для корректного вызова функций, и передает управление ядру на C++. Стек размещается в отдельной секции `.bss` которая автоматически обнуляется при загрузке.

Сcылки:

https://web.archive.org/web/20240920214648/https://wiki.osdev.org/GRUB

https://web.archive.org/web/20240915191242/https://www.gnu.org/software/grub/manual/grub/grub.html

https://web.archive.org/web/20240920214640/https://wiki.osdev.org/LD

https://web.archive.org/web/20240920214649/https://wiki.osdev.org/Linker_Scripts

https://web.archive.org/web/20240920214656/https://wiki.osdev.org/Multiboot

https://www.gnu.org/software/grub/manual/multiboot2/multiboot.html#Header-magic-fields

##### Настройка компилятора для встраиваемых систем

**Кросс-компиляция и кросс-компилятор**

При разработке операционной системы надо использовать **кросс-компиляцию** — процесс компиляции кода на одной платформе (хост-система, например, Linux) для выполнения на другой целевой платформе (наша новая ОС для x86). Обычный компилятор, встроенный в вашу систему, настроен для создания программ, которые работают в существующей ОС и используют её библиотеки и системные вызовы. Нам же нужен **кросс-компилятор** (`i686-elf-gcc`), который генерирует код, не зависящий от какой-либо конкретной операционной системы. Это абсолютно необходимо, так как наша ОС не имеет собственной стандартной библиотеки и среды выполнения во время загрузки.

**Отсутствие стандартной библиотеки (STL)**

Обратите внимание на флаги компиляции `-nostdinc` и `-nostdlib`. Они указывают компилятору **не использовать стандартные библиотеки Си (libc) и C++ (STL)**. Причина проста: этих библиотек не существует в среде нашей новой ОС. Функции вроде `printf`, `malloc` или `cout` зависят от сервисов хостовой ОС (например, системных вызовов Linux), которые в нашем ядре отсутствуют. Всю функциональность, включая работу с памятью, вводом-выводом и строками, нам придётся реализовывать самостоятельно.

**Объяснение ключевых флагов компиляции в `toolchain-x86.cmake`**

*   **`-ffreestanding`**: Указывает компилятору, что код будет работать в самостоятельной (freestanding) среде, а не в размещённой (hosted). Это отключает предположения компилятора о наличии стандартной библиотеки, но оставляет доступ к некоторым встроенным заголовкам (`<stdint.h>`, `<stddef.h>` и др.), необходимым для низкоуровневого программирования.
*   **`-m32`**: Генерировать 32-битный код. Это критически важно, так как загрузчик GRUB в рамках этого руководства загружает ядро в 32-битном защищённом режиме.
*   **`-nostdinc` / `-nostdlib`**: Запрещает компилятору и компоновщику использовать стандартные системные заголовки и библиотеки.
*   **`-fno-rtti`** (для C++): Отключает механизм RTTI (Run-Time Type Information), который для простого ядра не нужен и увеличивает размер бинарного файла.
*   **`-mgeneral-regs-only`** (для C++): Запрещает компилятору генерировать инструкции, использующие регистры SSE/MMX, что упрощает обработку контекста при переключении задач и прерываниях, так как не требуется сохранять состояние этих регистров.
*   **Флаги компоновщика (`OS_LINKER_FLAGS`)**:
    *   **`-T linker.ld`**: Использовать пользовательский скрипт компоновки для точного контроля над размещением секций ядра в памяти.
    *   **`-m elf_i386`**: Создавать выходной файл в формате 32-битного ELF.
    *   **`--nmagic`**: Отключает выравнивание секций по страницам памяти, что позволяет плотно упаковать ядро и уменьшить его размер.

Этот набор флагов и инструментов настраивает среду для создания чистого, независимого исполняемого файла ядра, который загрузчик GRUB сможет корректно загрузить и передать ему управление.